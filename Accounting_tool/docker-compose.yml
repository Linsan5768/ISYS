version: '3'

services:
  app:
    build: .
    ports:
      - "5002:5002"
    environment:
      - NODE_ENV=production
      - PORT=5002
    volumes:
      - db_data:/usr/src/app/backend/data
    restart: unless-stopped

volumes:
  db_data:
    driver: local 